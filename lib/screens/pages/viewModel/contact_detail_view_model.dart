
import '../pages.dart';

class ContactDetailViewModel {
  final ContactViewModel contact;
  final List<ContactViewModel> currentContactList;

  ContactDetailViewModel({required this.contact, required this.currentContactList});
}